{
  "name": "agent-loop-guard",
  "version": "0.1.0",
  "description": "Suspicious pattern detection for AI coding agent input/output. Node-only, no API keys, fully local.",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "agent-loop-guard": "./bin/agent-loop-guard.mjs"
  },
  "files": [
    "dist",
    "hooks",
    "bin",
    "adapters"
  ],
  "scripts": {
    "build": "tsc -p ./",
    "test": "jest && node --test hooks/cursor/extract.test.mjs hooks/cursor/post-tool-use-exit.test.mjs hooks/pre-tool-use-loop-breaker.test.mjs hooks/cursor/cursor-hook.test.mjs scripts/setup-claude.test.mjs",
    "setup:cursor": "node scripts/setup-cursor.mjs project",
    "setup:cursor:user": "node scripts/setup-cursor.mjs user",
    "setup:claude": "node scripts/setup-claude.mjs user",
    "setup:claude:project": "node scripts/setup-claude.mjs project"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3"
  }
}
